apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "css.fullname" . }}-test-connection"
  labels:
    {{- include "css.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "css.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
